# Development and test dependencies (install with: pip install -r requirements-dev.txt)
-r requirements.txt
pytest>=7.0
pytest-cov>=4.0
fpdf2>=2.7  # For PDF fixture generation (test fixtures)
black>=23.0  # Code formatting
ruff>=0.1  # Linter (CI + pre-commit)
mypy>=1.0  # Static type checker (CI + pre-commit)
pyyaml>=6.0  # Pipeline config parsing
beautifulsoup4>=4.12  # Downloader HTML parsing
openpyxl>=3.1  # Excel ingestion
pdfplumber>=0.10  # PDF text extraction
